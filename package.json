{
  "name": "@ai4ci/pi-vllm",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'"
  },
  "description": "vLLM extension for pi - discover, configure, and switch to local vLLM models",
  "keywords": ["pi-package", "vllm", "local-llm"],
  "pi": {
    "extensions": ["./vllm-local.ts"]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ai4ci/pi-vllm.git"
  },
  "license": "MIT",
  "dependencies": {
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "*"
  }
}
